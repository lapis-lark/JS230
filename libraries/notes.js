/*
  no matter the selector used with $(selector), an indexed JQ obj is returned (even with IDs)
  jQuery has getter/setter forms where the setter version just has a second argument:
    console.log($content.css('font-size'));    // getter
    $content.css('font-size', '18px');         // setter
  
  

*/